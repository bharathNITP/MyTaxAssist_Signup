import { create } from 'zustand';

interface notificationStoreState {
  // Define state types here
}

export const notificationStore = create<notificationStoreState>((set) => ({
  // Define initial state and actions
}));
