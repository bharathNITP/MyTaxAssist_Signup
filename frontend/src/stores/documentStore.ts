import { create } from 'zustand';

interface documentStoreState {
  // Define state types here
}

export const documentStore = create<documentStoreState>((set) => ({
  // Define initial state and actions
}));
